# Injected at build time via vite.config.js (not VITE_* — avoids Vercel public-prefix warnings).
# GameAnalytics web SDK still embeds these in the client bundle at build time.
GA_GAME_KEY=
GA_SECRET_KEY=
